写在前面
如果你也打算来北京玩——不管是一个人、和朋友、还是带娃——你一定经历过这种崩溃——
故宫提前7天放票，晚上8点整开抢，3秒秒光。
国家博物馆提前7天，下午5点放票，系统卡到你怀疑人生。
雍和宫、恭王府、毛主席纪念堂……每个景点的放票时间不一样、提前天数不一样、预约渠道不一样、儿童政策不一样。
你在手机备忘录里密密麻麻记了一堆时间，结果还是搞混了，错过了故宫的放票窗口。
别慌。我做了个小工具，专门解决这个问题。


今天这篇文章，我想跟你聊聊这个工具是怎么从"一个家长的焦虑"变成"所有人都能用的北京景点预约助手"的。不聊高深技术，就聊一个普通人怎么用代码解决一个真实的生活痛点。
一、故事的起点：一个家长的至暗时刻
七月的某个晚上，我坐在书桌前，手机屏幕上倒计时跳到 19:59:58。
故宫，今晚 20:00 放票，提前 7 天。
我提前把参观人信息填好了，日期选好了，场次选好了，手指悬在"提交"按钮上方，像个等待发令枪的短跑运动员。
20:00:00。
我点。
转圈。
20:00:03。
"当前预约人数过多，请稍后再试。"
20:00:08。
刷新。"今日门票已约满。"
8 秒。故宫 7 月 31 日的票，8 秒就没了。
那天晚上我盯着手机发了 5 分钟呆，然后做了一个决定：
我要写个程序，把这件事管起来。
不是因为我是程序员——我不是。我是一个带娃来北京玩的普通家长，只是恰好会一点点代码。我的诉求很简单：别让我再手动记那些放票时间了，别让我再错过窗口了，别让我在焦虑里反复横跳了。

二、第一版：给自己用的"作战指挥中心"
第一版做得很粗糙，但很实用。
我把自己的行程——哪一天到京、哪一天离京、想去故宫国博军博——全部写死在代码里，然后做了这么几个东西：
倒计时：自动找最近的放票时间，天/时/分/秒实时跳动
捡漏监控：放票后 15 分钟是退票高峰，我画了个柱状图，标注"第 5 分钟第一波回流""第 15 分钟主回流波（约 300-500 张！）"
操作清单：每天该干什么，几点干，用 checkbox 打勾，刷新不丢
策略卡片：每个景点的渠道、票价、儿童政策、闭馆日、爽约惩罚，一页全有
说白了，就是一个个人作战指挥中心。暗黑科技风的界面，满屏的倒计时和数据，看着像个 NASA 控制台。
它确实帮我约上了军博的票。那天晚上 17:00，我用它盯着国博的放票窗口，反复提交，终于在卡顿中挤了进去。
但说实话，它只对我一个人有用。行程写死了，景点写死了，换个人来完全用不了。
三、转折：为什么不把它开放给所有人？
约完票的那天晚上，我躺在酒店床上刷手机，看到小红书上一堆人在问：
"故宫到底提前几天放票？" "国博下午场好约还是上午场好约？" "带孩子去军博需要预约吗？儿童怎么算？"
这些问题我全查过、全整理过、全写进了代码里。
如果把这些信息开放出来，让每个来北京玩的人都能用呢？
这个念头一冒出来就收不住了。第二天一早，我就开始改造。

四、从"个人工具"到"通用产品"：一天的重构
改造的核心思路很简单——三步向导：
第一步：填出行信息。到京日期、玩几天、几个大人几个小孩、小孩多大。
第二步：选景点。12 个北京热门预约景点排成卡片，想去的勾上就行。每张卡片带难度标签（⭐⭐⭐⭐⭐ 最难预约）、提前天数、放票时间。
第三步：确认生成。系统自动检查有没有冲突——闭馆日、时间重叠、已过期的放票时间——然后一键生成完整的预约方案。
生成的方案长什么样呢？就是你前面看到的那一套：状态看板、倒计时、捡漏监控、时间线、策略卡片、操作清单、儿童票指南、日程推荐……全部根据你填的行程动态生成。
12 个景点的数据，是整个应用的灵魂。
每一个景点，都是一个这样的数据对象：
const ATTRACTIONS = {
  gugong: {
    name: '故宫博物院', icon: '🏯',
    channel: '微信小程序「故宫博物院」（唯一官方）',
    bookingDays: 7,           // 提前7天放票
    releaseTimes: [{hour: 20, minute: 0}],   // 晚上8点
    price: '旺季60元/淡季40元 · 18岁以下免费',
    childPolicy: '18岁以下免费，需预约0元票',
    closedDays: [1],          // 周一闭馆
    difficulty: 'hard',
    strategy: [
      '19:58 进入购票页面，选好日期+上午场，预填所有信息',
      '20:00 整立刻提交！先选大门票，别纠结珍宝馆钟表馆',
      '约不到别放弃：20:05-20:30 持续刷新捡漏回流票',
    ],
    // ...还有捡漏监控的配置
  },
  // ...其余11个景点
};

看到没？这不是什么高深的算法，就是把真实世界的信息，老老实实地写进代码里。
故宫提前 7 天放票、晚上 8 点、周一闭馆、18 岁以下免费——这些信息散落在各个景点官网、小程序、公告里，大家要花好几个小时才能查全。而我做的事情，就是把它整理成一个结构化的数据库，让程序来帮你算。
技术不酷，但它有用。
五、那个让我改了 18 处的词："抢票"
应用做完第一版，我发给自己看，越看越不对劲。
满屏都是"抢票"——"抢票作战指挥中心""抢票策略""抢票时间线""抢票成功""抢票核心策略"……
"抢"这个字，太焦虑了。
它传递的信息是：这是一个零和博弈，你抢到了就有人抢不到，你得快、你得狠、你得争。
但我做这个工具的初衷不是让大家更焦虑，而是让大家安心。知道什么时候该做什么，心里有底，不慌。
所以我把全文 18 处"抢票"相关的字眼，一个一个换掉了：
原来改后
抢票规划助手北京景点预约助手
抢票作战指挥中心安心预约指挥中心
抢票策略预约策略
抢票时间线预约时间线
抢票成功预约顺利
抢票窗口放票窗口
抢票实战经验预约实用经验
最难抢最难预约
待抢待预约
已抢到已约上
改完之后，整个应用的气质都变了。从"战场"变成了"客厅"，从"冲冲冲"变成了"别急，我帮你规划好了"。
代码是有温度的。你选什么词，就是在选择你想给用户什么感受。
六、踩过的坑（以及怎么爬出来的）
做项目最真实的部分，不是那些漂亮的功能，而是那些让你抓狂的 bug。挑几个印象深的聊聊。
坑一：计数器显示 12861，我自己都信了
我给应用加了个"已有 XX 位用户正在使用"的计数器，想着让新用户觉得"哦，有人在用，不是死项目"。设了个基准值 128，每位新访客 +1，存在浏览器本地。
第一次测试，页面上赫然显示：已有 12,861 位用户正在使用。
我：？？？
这才刚上线，哪来一万多人？
排查后发现：之前调试时我把基准值设成了 12860，浏览器 localStorage 里已经存了这个数。后来我把基准值改成 128，但代码读到的是存储里的旧值，新基准值根本没生效。
解决方案：加了版本号机制。
const KEY_VERSION = 'bbj_counter_version';
const CURRENT_VERSION = '2';

// 版本不一致 → 重置到新基准值
if (localStorage.getItem(KEY_VERSION) !== CURRENT_VERSION) {
  localStorage.setItem(KEY_COUNT, BASE_COUNT);
  localStorage.setItem(KEY_VERSION, CURRENT_VERSION);
  localStorage.removeItem(KEY_VISITED);   // 让老用户重新 +1
}

以后再想调基准值，把 CURRENT_VERSION 从 '2' 改成 '3'，所有老访客的本地存储就会被自动重置。
教训：localStorage 是一把双刃剑。它能帮你记住用户数据，也能帮你记住你不想记住的脏数据。改了默认值别忘了处理旧数据的迁移。

但故事到这里还没完。
后来有一天，我盯着页面上"已有 136 位用户正在使用"这行字，越看越不对劲。
136 是真的吗？
不是。这个数字是每个访客自己浏览器里的 localStorage 累加值。张三看到的可能是 136，李四看到的可能是 129。它看起来像社交证明，实际上每个用户看到的是自己的独享数字——你永远不知道真实有多少人在用。
一个工具如果连"有多少人在用"都在骗人，那它的其他信息还能信吗？
我做了一个决定：把假计数器整个删掉，接入真实的访客统计。这个故事，在下面的坑五里接着讲。
坑二："重新规划"按钮点了没反应
用户反馈：点击"重新规划"按钮，页面纹丝不动。
我查了半天代码，逻辑没问题啊——resetPlan() 函数写得好好的，清数据、回首页、重置表单……
最后发现是 confirm() 的锅。
function resetPlan() {
  if (!confirm('确定要重新规划吗？')) return;   // ← 罪魁祸首
  // ...重置逻辑
}

window.confirm() 弹出个确认框，用户点"确定"才执行。但在某些浏览器（尤其是微信内置浏览器）的预览/嵌入环境里，confirm() 会被静默拦截，直接返回 false，函数还没跑就 return 了。
解决方案：去掉 confirm，点击直接重置。要防误触的话，可以改用自定义的弹窗 UI，而不是依赖浏览器原生弹窗。
function resetPlan() {
  // 不要 confirm，直接重置
  localStorage.removeItem('userTrip');
  localStorage.removeItem('plan_checklist');
  // ...清空数据，回到第一步
}

教训：浏览器原生弹窗（confirm、alert、prompt）在移动端极不可靠。能用自定义 UI 替代的，就别用原生的。
坑三：过期信息刷屏，淹没了真正重要的内容
有用户截图给我看：预约时间线那一块，密密麻麻列了一堆"放票时间已过，可能无法预约"，把下面真正有用的策略和操作清单全挤到屏幕底下了。
原来我的冲突检测逻辑是这样的——每个过期的日期单独生成一条警告。假设你选了故宫，游玩 7 天，其中 6 天的放票时间已过，那就刷出来 6 条黄色警告。
解决方案：按景点聚合。
// 改之前：6 条警告
// 故宫 7/25 放票时间已过
// 故宫 7/26 放票时间已过
// 故宫 7/27 放票时间已过
// ...

// 改之后：1 条摘要
// 故宫博物院 7/25 至 7/30 共 6 个放票时间已过

更进一步，时间线和操作清单里直接过滤掉已过期的窗口：
// 只保留还没发生的预约窗口
const futureWindows = bookingWindows.filter(w => w.ts >= now);
bookingWindows.splice(0, bookingWindows.length, ...futureWindows);

一行 filter，页面瞬间清爽。用户看到的只有"接下来该做什么"，而不是"你错过了什么"。
教训：信息展示的核心不是"全"，而是"准"。用户不需要知道所有事情，只需要知道现在该做什么。过期的信息该藏就藏，别让它们占着黄金位置刷存在感。
坑四：连不上 GitHub，代码推不上去
项目做好了，要部署上线。但我的本地 Git 死活连不上 github.com——网络环境你懂的。
如果按常规流程，git push 失败，项目就卡在这里了。
解决方案：绕过 Git，直接用 GitHub REST API 推文件。
GitHub 提供了一套 Contents API，可以直接通过 HTTP 请求读写仓库文件。读文件 → Base64 编码 → 调 API 写入仓库，完美绕过了 Git 的网络问题。
# 伪代码示意
import base64, requests

with open('index.html', 'rb') as f:
    content = base64.b64encode(f.read()).decode()

requests.put(
    f'https://api.github.com/repos/{owner}/{repo}/contents/index.html',
    headers={'Authorization': f'token {TOKEN}'},
    json={'message': 'feat: 安心预约助手 v1.0', 'content': content, 'branch': 'main'}
)

然后调一次 Pages API 开启静态托管，40 秒后线上就能访问了。
教训：当一条路走不通时，别死磕，去找另一条路。Git 推不了就用 API，API 能做的事情远比你想象的多。
坑五：假计数器的终结——接入真实访客统计的三重坑
前面坑一说到，那个基于 localStorage 的假计数器一直让我心里不踏实。终于，我决定把它换成真实的访客统计。
选了 GoatCounter——免费、无 Cookie、隐私友好，能在页面上直接显示真实访客数。听上去很简单，接入过程却让我怀疑人生。
第一重坑：site code 不是账户名
注册 GoatCounter 时，我的账户登录名是 zhangdaka。我理所当然地把 zhangdaka 填进了代码里：
<!-- 错：用的是登录名 -->
<script data-goatcounter="https://zhangdaka.goatcounter.com/count"
        async src="//gc.zgo.at/count.js"></script>

结果 counter API 返回：Error 400: no site at this domain。
排查后发现：GoatCounter 注册时还有一个 site code（站点代码），和账户登录名是两回事。我的 site code 是 daka-booking，登录名是 zhangdaka。代码里要用 site code，不是登录名。
<!-- 对：用的是 site code -->
<script data-goatcounter="https://daka-booking.goatcounter.com/count"
        async src="//gc.zgo.at/count.js"></script>

教训：注册任何第三方服务，搞清楚"账户名""站点代码""API key"这几个概念的区别。它们看着像一回事，填错了就是 400。
第二重坑：API 返回的数字是字符串
site code 改对了，counter API 终于返回 200。但页面上还是显示"—"。
查了一下 API 返回值：
{"count_unique":"5", "count":"5"}

注意看——"5" 带引号，是字符串不是数字。而我的代码里写的是：
if (el && typeof data.count === 'number') {
  el.textContent = data.count.toLocaleString();
}

typeof "5" === 'number'？当然不是。条件永远为 false，数字永远显示不出来。
修复很简单：
const count = data.count === undefined ? NaN : Number(data.count);
if (el && !isNaN(count)) {
  el.textContent = count.toLocaleString();
}

Number("5") 会把字符串转成数字 5，isNaN 检查比 typeof 更可靠。
教训：别假设 API 返回的数据类型。JSON 里 "5" 和 5 是两回事，typeof 检查在动态类型语言里是个陷阱。用 Number() 显式转换 + isNaN() 校验，比信任 typeof 靠谱得多。
第三重坑：404 迷案——路径、跨域、Site URL
前两重坑填完，count.js 的 POST 请求成功返回 200（访问记录已经发出去了），但 counter API 读取计数时却返回 404。
发送成功了，读取却 404？这说不通。
我花了两个小时，试了无数种 URL 路径组合：
// 试过这些，有的 200 有的 404：
'https://daka-booking.goatcounter.com/counter//BookingAssistant/.json'      // 双斜杠 → 200 但 count=0
'https://daka-booking.goatcounter.com/counter/%2FBookingAssistant%2F.json'  // 末尾带斜杠 → 404
'https://daka-booking.goatcounter.com/counter/%2FBookingAssistant.json'     // 不带末尾斜杠 → 200

路径改对了，API 返回 200，但 count 还是 0。浏览器 Network 面板里看得很清楚——count.js 加载成功，POST /count 请求 200 OK，访问记录确实发出去了。可 counter API 就是读不到。
最终发现，问题不在代码里，而在 GoatCounter 的 Settings 页面有三个必须同时配对的设置：
设置项错误值正确值
Your sitehttps://daka-agent.github.iohttps://daka-agent.github.io/BookingAssistant/
URLs that can embed GoatCounter in a frame（空）https://daka-agent.github.io
Allow adding visitor counts on your website❌ 未勾选✅
第一个设置缺了 /BookingAssistant/ 子路径，导致 GoatCounter 虽然收到了访问记录（因为 count.js 自己发送了完整的 path），但 counter API 在站点配置层面匹配不到。
第二个设置留空，导致跨域请求被拒——页面的 fetch() 请求被 GoatCounter 的 CORS 策略挡住了。
第三个开关没打开，counter API 直接拒绝外部读取。
三个设置全部改对之后，counter API 立刻返回 {"count":"5"}，页面上也终于显示了真实数字。
教训：当 API 返回 404 但你确信数据已经发送成功时，别只盯着代码看。去检查服务端的配置——域名、路径、跨域白名单、权限开关。代码层面的问题你已经排除了，剩下的坑一定在配置里。
七、捡漏监控：这个功能我最得意
整个应用里我最喜欢的功能，是捡漏监控。
什么是捡漏？放票瞬间没约上，不代表彻底没戏。很多人抢到票后 15 分钟内不付款，订单会自动释放，这些票会回流到票池里——这就是"捡漏"的机会。
我根据实战经验，把放票后的 30 分钟拆成了 6 个时间节点：
scavengeConfig: {
  checkpoints: [
    {offset: 5,   label: '第一波小回流', desc: '部分用户未付款，订单开始释放'},
    {offset: 10,  label: '回流增大',     desc: '持续有人放弃，票量增多'},
    {offset: 15,  label: '🔥 主回流波', desc: '15分钟未付款订单集中释放，约300-500张！'},
    {offset: 20,  label: '次回流波',     desc: '后续未付款继续释放'},
    {offset: 25,  label: '尾波回流',     desc: '零散释放，还有机会'},
    {offset: 30,  label: '最后机会',     desc: '零星票源，最后冲刺'},
  ],
}

界面上用一个动态柱状图展示，高峰在第 15 分钟，柱子最高。配一个声音提醒，到了关键节点就"叮"一声。
这个功能不是什么技术奇迹，它是经验的结晶。你得真的抢过票、真的在 20:05 刷到过回流票、真的数过那几分钟的票量变化，才能做出这个时间线。
最好的代码，写的是生活经验。
八、移动端适配：因为大家都在微信里打开
做完桌面版我发现一个问题：大家不会在电脑上用这个工具。他们会在微信里收到一个链接，点开，在手机上看。
微信内置浏览器是一个很特殊的环境。它不是完整的 Chrome，有很多限制和怪癖。
我做了这些适配：
/* 适配刘海屏，内容不被圆角遮挡 */
padding: env(safe-area-inset-top) env(safe-area-inset-right)
         env(safe-area-inset-bottom) env(safe-area-inset-left);

/* 去掉 Android 点击时的灰色闪烁 */
-webkit-tap-highlight-color: transparent;

/* 去掉 300ms 点击延迟 */
touch-action: manipulation;

<!-- 微信顶栏颜色和应用主题一致 -->
<meta name="theme-color" content="#0a0e1a">

<!-- 防止 iOS 把数字识别成电话号码 -->
<meta name="format-detection" content="telephone=no">

<!-- 适配刘海屏 -->
<meta name="viewport" content="width=device-width, initial-scale=1.0,
    maximum-scale=1.0, user-scalable=no, viewport-fit=cover">

还有响应式断点：手机上景点选择从两列变一列、状态看板从三列变两列、表单从横排变纵堆叠……
这些细节单个看都不复杂，但加在一起，就是"在微信里打开体验不错"和"在微信里打开乱七八糟"的区别。
九、一些数字
1 个 HTML 文件，2000 行，84KB
12 个景点的完整数据库
18 处用词从"抢票"改为"安心预约"
7 个踩坑故事
1 个真实访客统计系统（GoatCounter，0 Cookie、0 后端）
1 天完成从个人工具到通用产品的改造
0 元服务器成本（GitHub Pages + GoatCounter 免费托管）
0 个后端接口（所有数据在浏览器本地）
线上地址在这里：https://daka-agent.github.io/BookingAssistant/
手机微信里直接打开就能用。
十、写在最后
做这个项目的过程中，我一直在想一个问题：
技术到底是为了什么？
如果是为了炫技，我应该去写一个分布式系统、一个微服务架构、一个用 Kubernetes 编排的云原生应用。那才"高级"。
但我选择写一个 84KB 的 HTML 文件，没有后端、没有数据库、没有框架。因为它解决的是一个真实的问题——一个家长晚上 8 点盯着手机抢故宫门票时的焦虑。
技术最动人的时刻，不是它多复杂、多前沿，而是它刚好在你需要的时候，帮了你一把。
一个妈妈在微信里打开链接，填好行程，看到"故宫 7 月 31 日晚上 8 点放票，提前 7 天"，心里一下子有底了。一个爸爸看到捡漏监控的柱状图，知道放票后 15 分钟还有机会，没有在 8 点 03 秒就放弃。
代码的温度，不在于它写了什么，而在于它为谁而写。
这个暑假，如果你也来北京玩，希望这个小工具能让你安心一点。
来北京玩的人，已经够焦虑了。能少一点，是一点。
📱 在线使用：https://daka-agent.github.io/BookingAssistant/
📦 开源代码：https://github.com/daka-agent/BookingAssistant
如果觉得有用，转发给身边要来北京的朋友们吧。让更多人少走弯路，就是这件事最大的意义。
